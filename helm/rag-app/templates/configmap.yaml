apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rag-app.configMapName" . }}
  labels:
    {{- include "rag-app.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
