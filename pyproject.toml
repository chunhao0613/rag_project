[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# 05 階段先擋語法/名稱類高風險問題，格式化規範留到後續階段。
select = ["E9", "F63", "F7", "F82"]
